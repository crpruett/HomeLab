apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mealie
spec:
  ingressClassName: traefik
  rules:
    - host: lds.crpruett.net
      http:
        paths:
          - backend:
              service:
                name: mealie
                port:
                  number: 9090
            path: /
            pathType: Prefix
